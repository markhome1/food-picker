# 复制为 .env 后填写。切勿将含真实 Key 的 .env 提交到公开仓库。

# 仅填「Web服务」Key（地理编码 / 静态图等）。H5 地图用 frontend/src/manifest.json 里的 JS API Key，二者分开配置即可。
# 若报 USERKEY_PLAT_NOMATCH，说明把 JS 专用 Key 误填到了此处。
AMAP_KEY=

# 截图识别店名（OCR Space 免费 key）：https://ocr.space/ocrapi
# OCR_SPACE_API_KEY=

# 本地开发可不填，默认使用 backend/data/food_picker.db
# 生产环境建议使用托管 Postgres（Neon / Vercel Postgres）
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require&channel_binding=require

# 使用 Postgres 时默认开启按「共享空间」登录隔离（每组 2～20 人，数据互不看见）；本地 SQLite 默认关闭。
# 若要本地 SQLite 也测登录：AUTH_ENABLED=true 且必须设置 JWT_SECRET。
# AUTH_ENABLED=false
# JWT_SECRET=请使用至少 32 字节的随机字符串
# JWT_EXP_DAYS=30

# 注册/加入前邮箱验证码（Resend：https://resend.com ，有免费额度）
# RESEND_API_KEY=re_xxxx
# 发件人：生产须用已验证域名，例如（与 Resend 里 fpick.asia 一致）：
# RESEND_FROM=noreply@fpick.asia
# 本地/沙箱测试仍可用 onboarding@resend.dev（仅能发到授权测试邮箱）
# EMAIL_OTP_TTL_MINUTES=15
